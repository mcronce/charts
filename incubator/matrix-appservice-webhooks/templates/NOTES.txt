The Matrix appservice for webhooks has been installed. You can access the server from within the k8s cluster using:

  {{ template "matrix-appservice-webhooks.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

WARNING:  Scaling the statefulset has not been tested.
