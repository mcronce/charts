The Matrix appservice for Discord has been installed. You can access the server from within the k8s cluster using:

  {{ template "matrix-appservice-discord.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}

WARNING:  Scaling the statefulset has not been tested.
